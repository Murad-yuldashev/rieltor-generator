import { PageHeading } from '@/shared/ui/page-heading';

/**
 * Placeholder landing route for the realtor cabinet shell. Later Phase 3.1 tasks
 * replace this with the become-realtor / dashboard flow.
 */
export function HomePage() {
  return (
    <main className="mx-auto min-h-dvh max-w-content bg-surface px-4 py-10">
      <PageHeading>RieltorApp — Rieltor kabineti</PageHeading>
    </main>
  );
}
